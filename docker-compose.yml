version: '3.8'

services:
  studybot:
    build: .
    container_name: studybot-app
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/data:/app/backend/data
    restart: always
